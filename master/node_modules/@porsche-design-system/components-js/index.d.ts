/**
 * @property prefix - the prefix used for the components
 */
export declare type LoadOptions = {
    prefix: string;
};
export declare const load: (opts?: LoadOptions) => void;


export declare const componentsReady: (el?: HTMLElement) => Promise<number>;
