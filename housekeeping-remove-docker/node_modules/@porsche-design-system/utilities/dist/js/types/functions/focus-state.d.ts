declare type PseudoElement = '::before' | '::after';
declare type Options = {
    color?: string;
    offset?: string;
    pseudo?: PseudoElement;
};
export declare const focus: (opts?: Options | undefined) => string;
export {};
